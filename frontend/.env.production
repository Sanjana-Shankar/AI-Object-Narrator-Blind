# Mobile/production build backend URL for the app.
# Pick ONE option and keep only one active VITE_BACKEND_URL line.

# Option 1: Android Emulator (recommended for quick local testing)
# VITE_BACKEND_URL=http://10.0.2.2:8000

# Option 2: Physical phone on same Wi-Fi as your laptop
# Replace with your laptop LAN IP address.
VITE_BACKEND_URL=http://172.24.224.1:8000

# Option 3: HTTPS tunnel (best for remote/mobile reliability)
# VITE_BACKEND_URL=https://your-backend-tunnel-url.example
